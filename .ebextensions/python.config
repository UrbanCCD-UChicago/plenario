option_settings:
  aws:elasticbeanstalk:container:python:
    WSGIPath: wsgi.py

commands:
  RunCeleryWorker:
    command: |
      if [ $(env | grep WORKER) ]; then
        python manage.py worker
      fi
